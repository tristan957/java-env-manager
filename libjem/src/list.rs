use std::error::Error;
use settings::{Distribution, Settings};

// pub fn list<'a>() -> Result<&'a mut Vec<Distribution>, Box<Error>> {
//     let settings = Settings::get()?;
//     Ok(settings.get_distributions())
// }
